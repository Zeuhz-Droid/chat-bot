import React from "react";
import "../Comp.css";

const ClientMessage = (props) => {
  return (
    <div className="ClientMessage">
      <p className="client-text">55</p>
    </div>
  );
};

export default ClientMessage;
